version: 1
env:
  variables:
    key: value
artifacts:
    baseDirectory: /
    files:
        - '**/*'
frontend:
  phases:
    preBuild:
      commands:
        - npm i
    build:
      commands:
        - npm run build