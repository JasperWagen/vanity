version: 1
# env:
#   variables:
#     key: value

frontend:
  artifacts:
      baseDirectory: /
      files:
          - '**/*'
  phases:
    preBuild:
      commands:
        - npm i
    build:
      commands:
        - npm run build